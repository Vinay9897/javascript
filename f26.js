// iterable

// iterable are those ,in which we can use loops
// string ,array are iterable
const firstName = "harshit";
for (let char of firstName) {
    console.log(char);
}

const items = ['item', 'items', 'items']
for (let word of firstName) {
    console.log(word);
}

// array like object
//those have length property (ex :-array and string)
// and those which we can access by index
// example : string
